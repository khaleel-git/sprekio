name = "sprekio-backend"
main = "src/index.ts"
compatibility_date = "2024-03-20"

# We will use the default workers.dev domain
workers_dev = true

# We will add D1 (Database) here later for cross-device syncing!
